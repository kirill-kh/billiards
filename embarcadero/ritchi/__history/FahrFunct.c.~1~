#define low 0
#define high 150
#definr step 10

#include <stdio.h>
#include <conio.h>

float celsium

main()
{
	float c, f;
	clrscr();
	for (c = low; c < high; c = c + step)
	{
		f = (9.0/5.0)*c + 32.0;
		printf("%6.1f %6.1f \n", f, c);
	}
	getch();
	return 0;
}